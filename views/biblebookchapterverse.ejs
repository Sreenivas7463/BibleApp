<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %> | <%= articles.book_name %> - <%= articles.chapter %>:<%= articles.verse %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
        <h1><%= articles.book_name %> - <%= articles.chapter %>:<%= articles.verse %></h1>
        <p>Welcome to <%= title %></p>

    
      <p class="display-4"><%= articles.text %></p>
    

</div>
</body>
</html>